android {
            defaultConfig {
                applicationId = "agenda.agenda.wds"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }